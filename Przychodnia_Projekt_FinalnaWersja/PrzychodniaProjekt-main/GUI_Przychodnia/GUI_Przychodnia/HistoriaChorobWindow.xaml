<Window x:Class="GUI_Przychodnia.HistoriaChorobWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GUI_Przychodnia"
        mc:Ignorable="d"
        Title="HistoriaChorobWindow" Height="450" Width="800">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,20">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock x:Name="lblPacjentNazwa" Text="Imię i Nazwisko Pacjenta" 
                           FontSize="18" FontWeight="Bold"/>
                <TextBlock x:Name="lblPacjentPesel" Text="PESEL: 00000000000" 
                           Foreground="Gray"/>
            </StackPanel>

            <Button x:Name="btnZatwierdz" Content="ZATWIERDŹ" HorizontalAlignment="Right" VerticalAlignment="Top" Padding="15,5" Click="BtnZatwierdz_Click"/>
        </DockPanel>

        <GroupBox Grid.Row="1" Header="Lista chorób" Margin="0,0,0,10">
            <ListBox x:Name="lstChoroby" SelectionMode="Multiple" Margin="5">
            </ListBox>
        </GroupBox>

        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <Label Content="Nazwa choroby / Opis:"/>
            <TextBox x:Name="txtNowaChoroba" Height="30" VerticalContentAlignment="Center"/>
        </StackPanel>

        <UniformGrid Grid.Row="3" Columns="3" Height="40">
            <Button x:Name="btnDodaj" Content="DODAJ" Margin="5,0,5,0" Click="btnDodaj_Click" />
            <Button x:Name="btnUsun" Content="USUŃ" Margin="5,0" Click="btnUsun_Click" />
        </UniformGrid>
    </Grid>
</Window>

